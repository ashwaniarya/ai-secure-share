# Optional: point the SPA at a different API origin.
# Leave unset for local dev (the Vite proxy forwards /api to the backend) and
# for production (FastAPI serves the SPA and API from the same origin).
# VITE_API_BASE_URL=
